const { _success, _error, _notFound, _lack, _notLogin } = require('../utils/resModel');
const { _checkData } = require('../utils/checkData2');

module.exports = {
    _success, _error, _notFound, _lack, _notLogin, _checkData
}